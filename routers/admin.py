from django.contrib import admin
from .models import Router

# Register your models here.

admin.site.register(Router)
# Registering the Router model with the Django admin site allows you to manage Router objects through the admin interface.
